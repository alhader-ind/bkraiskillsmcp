{
  "mcpServers": {
    "skillsgem-remote-mcp": {
      "description": "SkillsGem AI Edge MCP Server. (Note: For Claude Desktop standard stdio support, you may need an SSE-to-Stdio bridge if Claude Desktop hasn't native HTTP SSE enabled for remote servers).",
      "url": "https://bkraiskillsmcp.pages.dev/api/mcp",
      "env": {
        "API_KEY": "YOUR_PRIVATE_KEY_IF_NEEDED"
      }
    },
    "skillsgem-local-bridge": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-sse-bridge",
        "--url",
        "https://bkraiskillsmcp.pages.dev/api/mcp"
      ]
    }
  }
}
